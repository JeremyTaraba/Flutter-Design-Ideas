import 'package:flutter/material.dart';

class SidesTab extends StatelessWidget {
  const SidesTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text("Sides Tab");
  }
}
